{% extends 'base.html.twig' %}

{% block body %}
    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}!</h1>

        <ul>
            {% for item in arrayTest %}
                <li>{{item | upper}}</li>
                {% else %}
                    <p>La liste des mots clés du programmeur est vide</p>
            {% endfor %}
        </ul>
    </div>
{% endblock %}